#include <stdio.h>

int main(){
    int numero;

    while (1) {
        printf("Digite um numero inteiro (0 para parar): ");
        scanf("%d", &numero);

        if (numero == 0)
            break;
        }

        return 0;
    }